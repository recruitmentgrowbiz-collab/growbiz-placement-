import { getRecruiterInterviews } from "@/features/internal/services/recruiter";
export const metadata = { title: "Recruiter Interviews | Grow Biz Jobs" };
export default async function RecruiterInterviewsPage(){const rows=await getRecruiterInterviews();return <div><h1 className="font-display text-[28px] font-bold text-ink">Interviews</h1><div className="mt-6 grid gap-3">{rows.map(i=><article key={i.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{i.mode} interview</h2><p className="mt-1 text-[14px] text-mist">{i.scheduledAt} - {i.status} - Coordinator: Grow Biz</p><label className="mt-4 block text-[13px] font-medium text-ink">Internal feedback<textarea className="mt-1 w-full rounded-card border border-line px-3 py-2" /></label></article>)}</div></div>}
