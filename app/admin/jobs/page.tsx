import { getAdminJobs } from "@/features/internal/services/admin";
export const metadata = { title: "Job Moderation | Grow Biz Jobs" };
export default async function AdminJobsPage(){const rows=await getAdminJobs();return <div><h1 className="font-display text-[28px] font-bold text-ink">Job Moderation</h1><div className="mt-5 flex flex-wrap gap-2">{["All","Flagged","Reported","Active","Removed","Expired"].map(f=><span key={f} className="rounded-pill border border-line bg-white px-3 py-2 text-[13px]">{f}</span>)}</div><div className="mt-6 grid gap-3">{rows.map(j=><article key={j.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{j.title}</h2><p className="mt-1 text-[14px] text-mist">{j.company} - {j.status} - published {j.publishedAt ?? "draft"}</p><div className="mt-4 flex flex-wrap gap-2">{["No Action","Flag","Pause/Remove","Restore"].map(a=><button key={a} className="rounded-pill border border-line px-3 py-2 text-[13px]">{a}</button>)}</div></article>)}</div></div>}
