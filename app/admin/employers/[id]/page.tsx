import { notFound } from "next/navigation";
import { getAdminEmployer } from "@/features/internal/services/admin";
export default async function AdminEmployerDetailPage({params}:{params:{id:string}}){const c=await getAdminEmployer(params.id);if(!c)notFound();return <div><h1 className="font-display text-[28px] font-bold text-ink">{c.name}</h1><p className="mt-2 text-[14.5px] text-mist">Verification history, internal notes and status decision UI.</p><div className="mt-6 grid gap-4 lg:grid-cols-2"><section className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">Company information</h2><p className="mt-2 text-[14px] text-mist">{c.description}</p></section><section className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">Verification action</h2><div className="mt-4 flex flex-wrap gap-2">{["Approve","Reject","Needs Review"].map(a=><button key={a} className="rounded-pill border border-line px-3 py-2 text-[13px]">{a}</button>)}</div><label className="mt-4 block text-[13px] font-medium">Internal admin notes<textarea className="mt-1 w-full rounded-card border border-line px-3 py-2" /></label></section></div></div>}
