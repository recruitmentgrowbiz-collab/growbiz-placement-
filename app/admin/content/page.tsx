import { getContentSections } from "@/features/internal/services/admin";
export const metadata = { title: "Content | Grow Biz Jobs" };
export default async function ContentPage(){const rows=await getContentSections();return <div><h1 className="font-display text-[28px] font-bold text-ink">Content & Configuration</h1><p className="mt-2 text-[14.5px] text-mist">Lightweight structured editor shell. No raw JSON CMS or persistence yet.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{rows.map(s=><article key={s} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{s}</h2><label className="mt-4 block text-[13px] font-medium">SEO Title<input className="mt-1 min-h-10 w-full rounded-card border border-line px-3" /></label><label className="mt-3 block text-[13px] font-medium">Meta Description<textarea className="mt-1 w-full rounded-card border border-line px-3 py-2" /></label></article>)}</div></div>}
