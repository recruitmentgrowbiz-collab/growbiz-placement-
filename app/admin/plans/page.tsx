import { getAdminPlans } from "@/features/internal/services/admin";
export const metadata = { title: "Plans | Grow Biz Jobs" };
export default async function PlansPage(){const rows=await getAdminPlans();return <div><h1 className="font-display text-[28px] font-bold text-ink">Plans</h1><p className="mt-2 text-[14.5px] text-mist">Internal structured plan editor. Public prices remain configurable.</p><div className="mt-6 grid gap-4 lg:grid-cols-2">{rows.map(p=><article key={p.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{p.name}</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{["price","currency","activeJobsLimit","candidateUnlockLimit","recruiterSeats"].map(k=><label key={k} className="text-[13px] font-medium">{k}<input defaultValue={(p as any)[k]} className="mt-1 min-h-10 w-full rounded-card border border-line px-3" /></label>)}</div></article>)}</div></div>}
