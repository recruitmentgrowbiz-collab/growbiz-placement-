import { searchRecruiterCandidates } from "@/features/internal/services/recruiter";
export const metadata = { title: "Recruiter Candidate Search | Grow Biz Jobs" };
export default async function RecruiterCandidatesPage(){const rows=await searchRecruiterCandidates();return <div><h1 className="font-display text-[28px] font-bold text-ink">Candidates</h1><p className="mt-2 text-[14.5px] text-mist">Search respects future candidate privacy and authorization rules.</p><div className="mt-5 grid gap-4 lg:grid-cols-2">{rows.map(c=><article key={c.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{c.candidateName}</h2><p className="mt-1 text-[14px] text-mist">{c.headline} - {c.experience} - {c.location}</p><div className="mt-4 flex flex-wrap gap-2"><button className="rounded-pill border border-line px-3 py-2 text-[13px]">Add to requisition</button><button className="rounded-pill border border-line px-3 py-2 text-[13px]">Mark shortlist-ready</button><button className="rounded-pill bg-plum-600 px-3 py-2 text-[13px] font-medium text-white">Submit to client</button></div></article>)}</div></div>}
