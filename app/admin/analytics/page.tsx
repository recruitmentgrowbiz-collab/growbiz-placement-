import { getAdminAnalytics } from "@/features/internal/services/admin";
export const metadata = { title: "Analytics | Grow Biz Jobs" };
export default async function AnalyticsPage(){const data=await getAdminAnalytics();return <div><h1 className="font-display text-[28px] font-bold text-ink">Analytics</h1><p className="mt-2 text-[14.5px] text-mist">Mock/development KPI architecture, not live company performance.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{Object.entries(data).map(([k,v])=><article key={k} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[17px] font-semibold capitalize text-ink">{k.replace(/([A-Z])/g," $1")}</h2><p className="mt-2 text-[14px] text-mist">{v}</p><div className="mt-4 h-2 rounded-pill bg-plum-50"><div className="h-2 w-2/3 rounded-pill bg-plum-600" /></div></article>)}</div></div>}
