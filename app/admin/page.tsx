import Link from "next/link";
import { getAdminDashboard } from "@/features/internal/services/admin";
export const metadata = { title: "Admin Console | Grow Biz Jobs" };
export default async function AdminPage(){const d=await getAdminDashboard();const cards=[["Pending Employer Verifications",d.companies.filter(c=>c.verificationStatus!=="verified").length],["Jobs Awaiting Review",d.jobs.length],["Open Safety Reports",d.reports.filter(r=>r.status==="Open").length],["Failed Payments",0],["Active Memberships",d.plans.length],["Open Support Cases",d.supportCases.filter(c=>c.status==="Open").length]];return <div><h1 className="font-display text-[28px] font-bold text-ink">Admin Overview</h1><p className="mt-2 text-[14.5px] text-mist">Separate platform operations console for verification, moderation, safety, plans, users, content and support.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([k,v])=><Link key={k} href="/admin" className="rounded-card border border-line bg-white p-5"><p className="text-[13px] text-mist">{k}</p><p className="mt-2 font-display text-[24px] font-bold text-ink">{v}</p></Link>)}</div></div>}
