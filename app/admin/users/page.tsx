import { getAdminUsers } from "@/features/internal/services/admin";
export const metadata = { title: "Users & Roles | Grow Biz Jobs" };
export default async function AdminUsersPage(){const rows=await getAdminUsers();return <div><h1 className="font-display text-[28px] font-bold text-ink">Users & Roles</h1><div className="mt-5 flex flex-wrap gap-2">{["Candidates","Employers","Recruiters","Admins"].map(f=><span key={f} className="rounded-pill border border-line bg-white px-3 py-2 text-[13px]">{f}</span>)}</div><div className="mt-6 grid gap-3">{rows.map(u=><article key={u.id} className="rounded-card border border-line bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="font-display text-[18px] font-semibold text-ink">{u.name}</h2><p className="text-[14px] text-mist">{u.email} - {u.createdAt}</p></div><span className="rounded-pill bg-plum-50 px-3 py-1 text-[12.5px] font-medium text-plum-700">{u.role}</span></div><button className="mt-4 rounded-pill border border-line px-3 py-2 text-[13px]">Prepare role change</button></article>)}</div></div>}
