import { getPlacements } from "@/features/internal/services/recruiter";
export const metadata = { title: "Placements | Grow Biz Jobs" };
export default async function PlacementsPage(){const rows=await getPlacements();return <div><h1 className="font-display text-[28px] font-bold text-ink">Placements</h1><p className="mt-2 text-[14.5px] text-mist">Commercial status is internal and never shown to candidates.</p><div className="mt-6 grid gap-3">{rows.map(p=><article key={p.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{p.candidate}</h2><p className="mt-1 text-[14px] text-mist">{p.employer} - {p.role} - Joining {p.joiningDate}</p><p className="mt-2 text-[13px] text-mist">{p.placementStatus} - {p.commercialStatus}</p></article>)}</div></div>}
