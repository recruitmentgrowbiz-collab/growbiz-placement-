import { getAdminPayments } from "@/features/internal/services/admin";
export const metadata = { title: "Payments | Grow Biz Jobs" };
export default async function PaymentsPage(){const rows=await getAdminPayments();return <div><h1 className="font-display text-[28px] font-bold text-ink">Payments</h1><p className="mt-2 text-[14.5px] text-mist">Gateway-agnostic payment status and reconciliation frontend.</p><div className="mt-6 grid gap-3">{rows.map(p=><article key={p.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{p.employer}</h2><p className="mt-1 text-[14px] text-mist">{p.plan} - {p.amount} {p.currency} - {p.status}</p><p className="mt-2 text-[13px] text-mist">Paid entitlements may be restricted for failed/expired states later.</p></article>)}</div></div>}
