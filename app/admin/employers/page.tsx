import Link from "next/link";
import { getAdminEmployers } from "@/features/internal/services/admin";
export const metadata = { title: "Employer Verification | Grow Biz Jobs" };
export default async function AdminEmployersPage(){const rows=await getAdminEmployers();return <div><h1 className="font-display text-[28px] font-bold text-ink">Employer Verification</h1><div className="mt-5 flex flex-wrap gap-2">{["Pending","Verified","Needs Review","Rejected"].map(f=><span key={f} className="rounded-pill border border-line bg-white px-3 py-2 text-[13px]">{f}</span>)}</div><div className="mt-6 grid gap-3">{rows.map(c=><Link key={c.id} href={`/admin/employers/${c.id}`} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{c.name}</h2><p className="mt-1 text-[14px] text-mist">{c.website} - {c.industry} - {c.locations.join(", ")}</p><span className="mt-3 inline-flex rounded-pill bg-plum-50 px-3 py-1 text-[12.5px] font-medium text-plum-700">{c.verificationStatus}</span></Link>)}</div></div>}
