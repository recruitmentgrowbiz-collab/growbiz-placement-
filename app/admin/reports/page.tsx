import { getSafetyReports } from "@/features/internal/services/admin";
export const metadata = { title: "Safety Reports | Grow Biz Jobs" };
export default async function AdminReportsPage(){const rows=await getSafetyReports();return <div><h1 className="font-display text-[28px] font-bold text-ink">Safety Reports</h1><div className="mt-6 grid gap-3">{rows.map(r=><article key={r.id} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[18px] font-semibold text-ink">{r.category}</h2><p className="mt-1 text-[14px] text-mist">{r.target} - {r.createdAt} - {r.owner}</p><label className="mt-4 block text-[13px] font-medium">Internal note<input className="mt-1 min-h-10 w-full rounded-card border border-line px-3" /></label></article>)}</div></div>}
