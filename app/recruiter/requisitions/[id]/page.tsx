import { notFound } from "next/navigation";
import { getRequisition } from "@/features/internal/services/recruiter";
export default async function RequisitionDetailPage({params}:{params:{id:string}}){const r=await getRequisition(params.id);if(!r)notFound();const sections=["Overview","Client Requirement","Sourcing","Candidates","Shortlist","Interviews","Placement","Activity","Client Notes"];return <div><h1 className="font-display text-[28px] font-bold text-ink">{r.role}</h1><p className="mt-2 text-[14.5px] text-mist">{r.company} - {r.status} - {r.targetTimeline}</p><div className="mt-6 grid gap-4 lg:grid-cols-3">{sections.map(s=><section key={s} className="rounded-card border border-line bg-white p-5"><h2 className="font-display text-[17px] font-semibold text-ink">{s}</h2><p className="mt-2 text-[13.5px] text-mist">Frontend-ready {s.toLowerCase()} workspace.</p></section>)}</div></div>}
